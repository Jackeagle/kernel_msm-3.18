#include <iostream>
#include <errno.h>
#include <wiringPiI2C.h>

#define MAG_ADDR 0x1e

using namespace std;

int main(){
	//initialise i2c
	int fd, result;
	fd = wiringPiI2CSetup(0x19);
	//file driectory = fd
	cout << "init reslt: " << fd << endl;
	/*
	for(int i = 0; i< 0x0000ffff; i++){
	result = wiringPiI2CWriteReg16(fd, 0x19, (i & 0xfff));

		if(result == -1){
			cout << "errror. errno is: " << errno << endl;
		}
	}*/
}
